<?xml version="1.0" encoding="UTF-8"?>
<project name="${project.name}">

    <skin>
        <groupId>com.googlecode.fluido-skin</groupId>
        <artifactId>fluido-skin</artifactId>
        <version>1.3</version>
    </skin>

    <bannerLeft>
        <name>${project.artifactId}</name>
        <src>images/logo.png</src>
        <href>${project.url}/index.html</href>
    </bannerLeft>

    <version position="left"/>

    <body>
        <menu name="${project.name}">
            <item name="Introduction" href="index.html"/>
        </menu>

        <menu name="Miscellaneous">
            <item name="PDF Manual" href="${project.artifactId}-${project.version}.pdf"/>
        </menu>

        <menu ref="reports"/>

        <menu name="External Links">
            <item name="sameas.org" href="http://sameas.org/"/>
        </menu>
    </body>

</project>
